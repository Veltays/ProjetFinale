<Page x:Class="ProjetFinale.WPF.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Settings"
      Background="#303030">

    <ScrollViewer Margin="20" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="800">

            <!-- Titre -->
            <TextBlock Text="SETTINGS"
                 Foreground="White"
                 FontSize="32"
                 FontWeight="Bold"
                 Margin="0,0,0,30"/>

            <!-- UNITÉS DE MESURE -->
            <Border Style="{StaticResource Settings.Card}">
                <StackPanel>
                    <TextBlock Text="UNITÉS DE MESURE" Style="{StaticResource Settings.SectionTitle}"/>

                    <!-- Format Poids -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FORMAT POIDS" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Choisissez l'unité d'affichage du poids"
                         Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <ComboBox Grid.Column="1"
                      x:Name="FormatPoidsComboBox"
                      Style="{StaticResource Settings.ComboBox}"
                      ItemContainerStyle="{StaticResource Settings.ComboBoxItem}"
                      SelectedIndex="0">
                            <ComboBoxItem Content="KG"/>
                            <ComboBoxItem Content="LBS"/>
                        </ComboBox>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FORMAT TAILLE" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Format d'affichage de la taille (cm ou inch)"
               Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <ComboBox Grid.Column="1"
            x:Name="FormatTailleComboBox"
            Style="{StaticResource Settings.ComboBox}"
            ItemContainerStyle="{StaticResource Settings.ComboBoxItem}"
            SelectedIndex="0">
                            <ComboBoxItem Content="CM"/>
                            <ComboBoxItem Content="INCH"/>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- APPARENCE -->
            <Border Style="{StaticResource Settings.Card}">
                <StackPanel>
                    <TextBlock Text="APPARENCE" Style="{StaticResource Settings.SectionTitle}"/>

                    <!-- Mode sombre -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="MODE SOMBRE" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Activer le thème sombre (recommandé)"
                         Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <CheckBox Grid.Column="1"
                      x:Name="DarkModeToggle"
                      Style="{StaticResource Settings.ToggleSwitch}"
                      IsChecked="True"/>
                    </Grid>

                    <!-- Thème couleur -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="THÈME COULEUR" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Couleur principale de l'interface"
                         Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <ComboBox Grid.Column="1"
                      x:Name="ThemeColorComboBox"
                      Style="{StaticResource Settings.ComboBox}"
                      ItemContainerStyle="{StaticResource Settings.ComboBoxItem}"
                      SelectedIndex="0">
                            <ComboBoxItem Content="Violet"/>
                            <ComboBoxItem Content="Bleu"/>
                            <ComboBoxItem Content="Rose"/>
                            <ComboBoxItem Content="Vert"/>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- NOTIFICATIONS -->
            <Border Style="{StaticResource Settings.Card}">
                <StackPanel>
                    <TextBlock Text="NOTIFICATIONS" Style="{StaticResource Settings.SectionTitle}"/>

                    <!-- Rappels d'entraînement -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="RAPPELS D'ENTRAÎNEMENT" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Recevoir des notifications pour les séances programmées"
                         Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <CheckBox Grid.Column="1"
                      x:Name="WorkoutRemindersToggle"
                      Style="{StaticResource Settings.ToggleSwitch}"
                      IsChecked="True"/>
                    </Grid>

                    <!-- Rappels objectifs -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="RAPPELS OBJECTIFS" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Notifications de progression vers vos objectifs"
                         Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <CheckBox Grid.Column="1"
                      x:Name="GoalRemindersToggle"
                      Style="{StaticResource Settings.ToggleSwitch}"
                      IsChecked="False"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- GESTION DES DONNÉES -->
            <Border Style="{StaticResource Settings.Card}">
                <StackPanel>
                    <TextBlock Text="GESTION DES DONNÉES" Style="{StaticResource Settings.SectionTitle}"/>

                    <!-- Sauvegarde auto -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="SAUVEGARDE AUTOMATIQUE" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Sauvegarder automatiquement vos données"
                         Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <CheckBox Grid.Column="1"
                      x:Name="AutoSaveToggle"
                      Style="{StaticResource Settings.ToggleSwitch}"
                      IsChecked="True"/>
                    </Grid>

                    <!-- Fréquence de sauvegarde -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FRÉQUENCE DE SAUVEGARDE" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Intervalle de sauvegarde automatique"
                         Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <ComboBox Grid.Column="1"
                      x:Name="SaveFrequencyComboBox"
                      Style="{StaticResource Settings.ComboBox}"
                      ItemContainerStyle="{StaticResource Settings.ComboBoxItem}"
                      SelectedIndex="1">
                            <ComboBoxItem Content="1 min"/>
                            <ComboBoxItem Content="5 min"/>
                            <ComboBoxItem Content="15 min"/>
                            <ComboBoxItem Content="30 min"/>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- COMPTE UTILISATEUR -->
            <Border Style="{StaticResource Settings.Card}">
                <StackPanel>
                    <TextBlock Text="COMPTE UTILISATEUR" Style="{StaticResource Settings.SectionTitle}"/>

                    <!-- Modifier profil -->
                    <Grid Margin="0,0,0,25">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="MODIFIER PROFIL" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Mettre à jour vos informations personnelles"
                         Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <Button Grid.Column="1"
                    Content="MODIFIER"
                    Style="{StaticResource Settings.Button.Primary}"
                    Width="120"
                    x:Name="ModifierProfilButton"/>
                    </Grid>

                    <!-- Zone de danger -->
                    <Border Background="#330000" CornerRadius="8" Padding="20">
                        <StackPanel>
                            <TextBlock Text="⚠️ ZONE DE DANGER"
                         Foreground="#FF6B6B"
                         FontSize="16"
                         FontWeight="Bold"
                         Margin="0,0,0,15"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="SUPPRIMER LE COMPTE"
                             Foreground="#FF6B6B"
                             FontSize="14"
                             FontWeight="Bold"/>
                                    <TextBlock Text="Cette action est irréversible. Toutes vos données seront perdues."
                             Foreground="#FFAAAA"
                             FontSize="12"
                             Margin="0,5,0,0"/>
                                </StackPanel>

                                <Button Grid.Column="1"
                        Content="SUPPRIMER"
                        Style="{StaticResource Settings.Button.Danger}"
                        Width="120"
                        x:Name="DeleteAccountButton"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
