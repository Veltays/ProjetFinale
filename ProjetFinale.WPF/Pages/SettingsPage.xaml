<Page x:Class="ProjetFinale.WPF.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Settings"
      Background="{DynamicResource App.Background}"
      Foreground="{DynamicResource App.Foreground}">

    <ScrollViewer Margin="20" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="800">

            <!-- Titre -->
            <TextBlock Text="SETTINGS"
                       FontSize="32"
                       FontWeight="Bold"
                       Margin="0,0,0,30"
                       Foreground="{StaticResource Settings.Brush.TextPrimary}"/>

            <!-- UNITÉS DE MESURE -->
            <Border Style="{StaticResource Settings.Card}">
                <StackPanel>
                    <TextBlock Text="UNITÉS DE MESURE" Style="{StaticResource Settings.SectionTitle}"/>

                    <!-- Format Poids -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FORMAT POIDS" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Choisissez l'unité d'affichage du poids" Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <ComboBox Grid.Column="1"
                                  x:Name="FormatPoidsComboBox"
                                  Style="{StaticResource Settings.ComboBox}"
                                  ItemContainerStyle="{StaticResource Settings.ComboBoxItem}"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="KG"/>
                            <ComboBoxItem Content="LBS"/>
                        </ComboBox>
                    </Grid>

                    <!-- Format Taille -->
                    <Grid Margin="0,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FORMAT TAILLE" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Format d'affichage de la taille (cm ou inch)" Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <ComboBox Grid.Column="1"
                                  x:Name="FormatTailleComboBox"
                                  Style="{StaticResource Settings.ComboBox}"
                                  ItemContainerStyle="{StaticResource Settings.ComboBoxItem}"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="CM"/>
                            <ComboBoxItem Content="INCH"/>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- APPARENCE -->
            <Border Style="{StaticResource Settings.Card}">
                <StackPanel>
                    <TextBlock Text="APPARENCE" Style="{StaticResource Settings.SectionTitle}"/>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="MODE SOMBRE" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Activer le thème sombre (recommandé)" Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <CheckBox Grid.Column="1"
                                  x:Name="DarkModeToggle"
                                  Style="{StaticResource Settings.ToggleSwitch}"
                                  IsChecked="True"/>
                    </Grid>


                </StackPanel>
            </Border>

            <!-- GESTION DES DONNÉES -->
            <Border Style="{StaticResource Settings.Card}">
                <StackPanel>
                    <TextBlock Text="GESTION DES DONNÉES" Style="{StaticResource Settings.SectionTitle}"/>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="SAUVEGARDE AUTOMATIQUE" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Sauvegarder automatiquement vos données" Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <CheckBox Grid.Column="1"
                                  x:Name="AutoSaveToggle"
                                  Style="{StaticResource Settings.ToggleSwitch}"
                                  IsChecked="True"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FRÉQUENCE DE SAUVEGARDE" Style="{StaticResource Settings.Label}"/>
                            <TextBlock Text="Intervalle de sauvegarde automatique" Style="{StaticResource Settings.Description}"/>
                        </StackPanel>

                        <ComboBox Grid.Column="1"
                                  x:Name="SaveFrequencyComboBox"
                                  Style="{StaticResource Settings.ComboBox}"
                                  ItemContainerStyle="{StaticResource Settings.ComboBoxItem}"
                                  SelectedIndex="1">
                            <ComboBoxItem Content="1 min"/>
                            <ComboBoxItem Content="5 min"/>
                            <ComboBoxItem Content="15 min"/>
                            <ComboBoxItem Content="30 min"/>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
